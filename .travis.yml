language: ruby
rvm:
 - 2.3.3
# uncomment and edit the following line if your project needs to run something other than `rake`:
script: bundle install --without production

notifications:
  email:
    recipients:
      - cdlormor+dev@gmail.com
